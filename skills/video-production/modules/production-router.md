# Production Router

1. Validate the Input Contract and confirm the staged approval state. Without Stage 0 approval, block before visual output; without Stage 1 approval, block before the full package and any Notion write. Before Clip compilation, run the deterministic Scene Continuity and Feasibility gates and preserve open semantic states.
2. Resolve only ready Style Profiles and record their registry IDs and source provenance.
3. Load Adapter capabilities without inheriting legacy profile production syntax.
4. After Stage 0 approval, compile the minimum visual test package: Global visual DNA plus `主角`, `场景`, and `Key Frame` image prompts. Stop for Stage 1 confirmation.
5. After Stage 1 approval, freeze `semantic_locks`, build Scene and Clip plans, then create the ADP-defined 3–5 LookDev anchors.
6. Set the Gate to `awaiting_user_approval` before any external call. Compile only the minimum approved Gate set after approval.
7. Route generated results through QA, continuity, and targeted retry. Project a review-only `Production_Package.md` from the manifest, then project to Notion only after Stage 1 confirmation.

When any prerequisite is missing, return a specific block reason; never silently substitute a Fixture or a planned state for evidence. A v1.5 manifest is read-only migration input; a v1.6 revision is written alongside it.
