# Keyframe Executable Prompt — 5 modules

```text
【画面】
第一句：画面主体＋当前动作＋关键可见状态。

【参考继承】
标明身份、场景、道具、实际起始状态和 Visual Baseline 的各自职责。

【构图光学】
取景、机位、视角、空间关系；按触发条件加入焦段、景深和透视。

【光线色彩】
光线的情绪/可读性结果；继承或显式风格；当前颜色关系和 Grade。

【连续约束】
锁定项：...
允许变化：...
禁止项：...
```

References stay high-priority production input but cannot displace the current image from the first effective prompt sentence.

For `prompt_variant=cover_visual`, append target platform, platform-native composition, title-safe zone, UI avoidance, thumbnail priority, and checksum-bound reference roles. Reserve clean title space but render no exact Chinese, badge, dark title board, border, watermark, or test label unless an approved Series rule explicitly requires it. Xiaohongshu and Douyin must compile independent composition clauses and prompt hashes; mechanical crop is not a variant.
