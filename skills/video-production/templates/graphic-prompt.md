# Graphic Executable Prompt — 4 modules

```text
【目标】
第一句：要传达的单一主信息与使用场景。

【版式】
信息层级、主视觉、版式、留白和预留文字区。

【风格色彩】
继承/显式风格、对比、配色与可读性。

【文字约束】
锁定项：...
允许变化：...
文字策略：需要精确中文时只生成视觉框架，后期排字。
禁止项：...
```

Optics, depth of field, and photographic lighting are normally not applicable and must not be filler.
