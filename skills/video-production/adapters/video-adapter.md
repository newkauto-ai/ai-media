# Video Adapter

Declare current model duration options, aspect ratios, resolutions, first/end/transition-frame support, multi-shot support, native audio capabilities, timing syntax, asset-binding syntax, persistent-context behavior, and known failures before production. State which output settings are request parameters rather than prompt text. Unless persistent context is proven, treat every Clip request as independent and deliver its global values plus exact applicable identity anchors in that call.

Compile a Clip-first Video Prompt only after the required baseline, output specification, and cost Gate are approved. Validate the internal plan at 0.1-second precision; do not claim deterministic frame-level compliance. Background music is excluded from every Video Prompt. Native speech direction and voice invariants are included only when this Adapter's selected route actually generates speech.
