# Production Package Review Projection

Render this from the manifest for Stage 2 review after `确认第2点`: immutable semantic locks; frozen script; Global/Domain Visual DNA; current Gate; LookDev anchor and Domain statuses; approved baseline references; Scene/Clip/audio plan; executable prompts; blocked items and reasons; precise cost approvals; QA/retry outcomes; Notion projection state; and unverified limitations. It is not a second machine source of truth and must not duplicate executable prompts.

Before Stage 1 approval, render only the visual test package: Global visual DNA and exactly three
image prompts (`主角`, `场景`, `Key Frame`). Do not write Notion from that preview.
