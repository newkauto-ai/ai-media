# Packaging Router

Require an explicit list of supported target platforms and emit no unselected overlay. Normalize each target to exactly one profile and overlay. YouTube Shorts and YouTube Long share the YouTube Base fields but keep distinct child overlays and cover specifications.

Read Skill 2 `platform_packaging` only when no Skill 5 copy is supplied. Record each consumed hint in `strategy.legacy_hints_consumed`; final copy authority remains `publishing_packaging`.
