# Performance Advisory

Report only qualitative `strong | mixed | weak | unknown` dimensions with evidence. `Qualified Click` is a hypothesis label, never a numeric score. Pre-publish logic does not predict retention. Promise mismatch, unreadable cover, or irrelevant metadata may block Ready; ordinary performance uncertainty is advisory.
